# Never deploy local dependency or build scratch directories.
**/node_modules/**
**/.vite/**
**/coverage/**

# TypeScript incremental build state - regenerated by `npm run build`, and has
# no business being shipped inside the UI Bundle.
**/*.tsbuildinfo
